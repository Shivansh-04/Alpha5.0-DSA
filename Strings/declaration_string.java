// package Strings;

import java.util.Scanner;

public class declaration_string {
    public static void main(String[] args) {
        //! Strings Declaration
        Scanner sc = new Scanner(System.in);

        //! when you have to take single character as input we use only "sc.next()"
        String name = "dev"; //! we will use sc.next()
        //! but when we have to take full line of input we use "sc.nextLine()"
        String name1 = "dev gupta"; //! we will use sc.nextLine()

        


    }
}
